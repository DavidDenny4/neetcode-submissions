class Solution:
    def numDecodings(self, s: str) -> int:

        self.count = 0
        vals = [int(c) for c in s]
        print(f"vals is {vals}")

        def dfs(index):
            if index > len(s):
                return 0

            
        
        return 0

        
        
        
        
